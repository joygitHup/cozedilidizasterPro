# 边坡地质灾害智能预防管控平台 - 设计风格

## 气质与意象

**关键词**：专业、可靠、紧迫、数据驱动

想象一个省级应急管理指挥中心——深色大屏上跳动着实时数据，红色预警信号闪烁，工作人员在紧张有序地协调转移。这不是一个普通的后台管理系统，而是一个关乎生命安全的决策平台。

- 整体气质偏向**深色科技指挥舱**风格，但不做过度赛博朋克化
- 信息密度高，但层次分明——紧急信息用色彩和位置突出，常规信息安静排列
- 每一个数据点都暗示着"这可能是某座山上某个村庄的安全信号"

## 视觉策略

- **数据可视化优先**：图表、地图、仪表盘是核心视觉元素，不是装饰
- **状态色彩系统**：红/橙/黄/蓝 四级预警色贯穿全局，形成统一语义
- **地图为核心**：GIS 地图在多个页面中占据主导地位，数据叠加在地理空间上
- **图标语义化**：使用 Lucide 图标，每个图标都有明确的功能含义

## 配色方案

| 用途 | 色值 | 意象来源 |
|------|------|----------|
| 主背景 | `#0F172A` (slate-900) | 深夜指挥中心屏幕底色 |
| 卡片背景 | `#1E293B` (slate-800) | 控制台上独立显示面板 |
| 边框/分割 | `#334155` (slate-700) | 设备面板的金属边框 |
| 主文字 | `#F1F5F9` (slate-100) | 屏幕上的高亮数据 |
| 次要文字 | `#94A3B8` (slate-400) | 标签和辅助说明 |
| 红色预警 | `#EF4444` (red-500) | 最高等级警报——立即行动 |
| 橙色预警 | `#F97316` (orange-500) | 高度危险——准备响应 |
| 黄色预警 | `#EAB308` (yellow-500) | 需关注——持续监测 |
| 蓝色预警 | `#3B82F6` (blue-500) | 一般提示——常规信息 |
| 安全/正常 | `#22C55E` (green-500) | 设备在线、任务完成 |
| 主强调色 | `#06B6D4` (cyan-500) | 数据高亮、交互焦点 |

## 字体排版

- **中文**：`"PingFang SC", "Microsoft YaHei", sans-serif`
- **英文/数字**：`"Inter", "JetBrains Mono", monospace`（数字使用等宽字体便于对齐）
- **排版节奏**：
  - 页面标题：24px bold，带左侧色条标识
  - 卡片标题：16px semibold
  - 数据数值：使用大号数字（28-36px），突出关键指标
  - 表格数据：14px，紧凑排列

## 动效与交互

- **预警闪烁**：红色预警使用脉冲动画（pulse），引起注意但不刺眼
- **数据刷新**：数值变化时使用平滑过渡（300ms ease-out）
- **面板展开**：使用 slide + fade 组合，200ms
- **地图交互**：缩放、旋转保持原生流畅感，标注点出现使用 scale 动画
- **缓动曲线**：统一使用 `cubic-bezier(0.4, 0, 0.2, 1)`

## 设计禁忌

- **不要**使用浅色/白色背景——这是指挥系统，不是办公OA
- **不要**使用圆角过大的卡片（最大 rounded-lg，8px）——保持硬朗专业感
- **不要**使用渐变色作为主色调——色彩用于语义（预警等级），不是装饰
- **不要**使用卡通/可爱风格的图标——保持严肃专业
- **不要**在预警区域使用绿色——绿色只表示"安全/正常"
- **不要**过度使用动画——每个动效都应有信息含义
